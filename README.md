# AERIS Native CI

Public, isolated compile-validation harness for the `aeris-native-calls` Expo local module.

This repository contains no backend code, credentials, push-provider keys, user data, messages, or production configuration.
